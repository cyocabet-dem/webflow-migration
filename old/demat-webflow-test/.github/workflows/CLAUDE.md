# CLAUDE.md — `.github/workflows/`

GitHub Actions workflows for the old embed-code repo.

| File | Purpose |
|------|---------|
| `purge-cdn.yml` | Purges the jsDelivr CDN cache for the `demat-webflow` scripts on push, so Webflow serves the latest versions. |

Reference only. Not migrated to Nuxt.
