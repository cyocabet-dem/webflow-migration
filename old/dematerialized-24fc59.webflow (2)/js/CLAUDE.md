# CLAUDE.md — `js/`

JavaScript from the Webflow export. Read-only.

| File | Purpose |
|------|---------|
| `webflow.js` | The Webflow runtime, including IX2 (Interactions 2.0) animations and behaviors. |

Read only when reproducing a specific Webflow interaction. Reproduce essential interactions in Nuxt
with CSS or a light animation library; drop purely decorative ones with a note. The site's *own*
application logic is **not** here — it lives in `old/demat-webflow-test/`.
